import styles from "../../styles/EventListComponent.module.css";

const EventListComponent = () => {
  return (
  <div className={styles.eventListContainer}>
    <div className={styles.eventListHeader}>Events</div>
  </div>    
  );
};

export default EventListComponent;
